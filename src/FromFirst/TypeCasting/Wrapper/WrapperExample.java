package FromFirst.TypeCasting.Wrapper;

import java.util.Scanner;

public class WrapperExample {
	public static void main(String[] args) {
	/*	 Scanner sc = new Scanner(System.in);
		String str = "AmaResH 007";
		char[] ch = str.toCharArray();
		int upper_count = 0;
		int lower_count = 0;
		int digit_count = 0;
		int space_count = 0;
		for (int i = 0; i < ch.length; i++) {
			if (Character.isLowerCase(i)) {
				lower_count++;
			} else if (Character.isUpperCase(i)) {
				upper_count++;
			} else if (Character.isDigit(i)) {
				digit_count++;
			} else if (Character.isSpaceChar(i)) {
				space_count++;
			}
		}
		System.out.println("No. of lower cases:" + lower_count);
		System.out.println("No. of upper cases:" + upper_count);
		System.out.println("No. of digit:" + digit_count);
		System.out.println("No. of space:" + space_count);
	
*/
		/*String str = "AmaresH007";
		char[] arr = str.toCharArray();
		int ucount=0;
		int lcount=0;
		for(int i=0;i<arr.length;i++)
		{
			if(arr[i]>=)
		}*/
}
}